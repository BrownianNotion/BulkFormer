# installation shenanigans:
torch_sparse wouldn't detect the torch installation with uv, needed to do uv pip install
from pre-built wheel - is there a way to put this directly into requirements?

```
uv pip install torch-sparse -f https://data.pyg.org/whl/torch-2.5.1+cu121.html                                     
uv pip install torch-scatter -f https://data.pyg.org/whl/torch-2.5.1+cu121.html
```

# issues
- shape error 20 011 rows in gene info and one less in gene list, corrected using 
`bulkformer_gene_info = bulkformer_gene_info[bulkformer_gene_info['ensg_id'] != '35991']`
according to `https://github.com/KangBoming/BulkFormer/issues/1`

- directly applying to ucl data results in 0 dimension (shape [16, 0, 1920])on gene-level 
embedding, not sure what has gone wrong -> both datasets need to use gene symbols or ids.

# other
- weird EOEF errors - out of disk space, clearing pip caches/py helped.

```
rm -rf ~/.cache/pip/* 
find ~/BulkFormer/ -name "__pycache__" -type d -exec rm -rf {} + 
find ~/BulkFormer/ -name "*.pyc" -delete
```

- downloading files with wget; the default file name needs to have suffixes removed

# questions
1. Why is the shape of the gene-level embedding [16, n_genes, 1920] ?

2. What are expression values ? (gene expression bulk values, I think) Still not sure
what expression value embeddings refers to though.

3. How many of the gene symbols are in BulkFormer and our data? Appears that
`main_gene_selection` already accounts for missing genes in some sense. 
    19338 common genes

4. Why are max, mean and median embeddings added together in aggregate type all?

5. What is the difference between gene level and transcriptome level features? 

6. What is `high_var_gene_idx`? My guess is the top 2000 genes with most variance. Why is
it only used for transcriptome level features and not gene level?

7.  Why remove the module prefix? 
    Torch adds module prefix to all layers
    when wrapped in DP/DDP. Removing prefix ensures smooth running on single cpu/GPU.

8. Why is -10 the fill value for the missing genes?

9. The forward call of the model can not only specify outputs, but
the embeddings in hidden layer. Why are the embeddings calculated from hidden layer 2
and not the following starting embeddings?
```
x = self.expr_emb(x) + self.gene_emb_proj(self.gene_emb) + self.ae_enc(x).unsqueeze(1)
x = self.x_proj(x)
```

10. Following on from the previous question then, what is the actual output of
the model? Is output a probability distribution across genes? (Nope, col sum isnt 1)


# next steps notes 
1. Run classifiers on the learned embeddings for UCL dataset, figure out which embeddings
to use (probably the first one, since it has second dimension 640).
